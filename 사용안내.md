# 가족 축제 여행 프로그램

## 지금 실행
`여행지도_열기.bat`을 더블클릭하거나 `index.html`을 브라우저에서 여세요.
날짜, 월, 지역, 행사명으로 검색하고 하트로 여행 후보를 저장할 수 있습니다.
여행 날짜를 선택하면 해당 기간과 겹치는 행사를 보여줍니다.
찜 목록은 해당 브라우저에 저장됩니다. 예전 버전의 순번 기반 찜은 잘못 연결될 수 있어 가져오지 않습니다.

## 데이터 업데이트
Python 3.9 이상에서 `python festival_search.py`를 실행합니다. 추가 패키지는 필요 없습니다.
실행 위치와 관계없이 이 폴더의 data.js를 갱신합니다.
한국관광공사 API 전체 페이지에서 종료되지 않은 행사를 가져옵니다.
임시로 만든 야행 날짜는 사용하지 않습니다. 수집 실패 시 기존 정보를 보존합니다.
`.env`에는 기존 프로그램의 인증키를 옮겨 두었습니다. 이 파일은 업로드하지 마세요.

## 매주 월요일 오전 9시 자동 업데이트
`.github/workflows/update.yml`에 한국 시간 기준 월요일 오전 9시를 설정했습니다.
온라인 활성화는 아직 완료되지 않았습니다. GitHub 로그인이 필요합니다.
1. 기존 저장소에 이 폴더의 수정 파일을 반영합니다. `.env`는 제외합니다.
2. 저장소 Settings → Secrets and variables → Actions에서 TOUR_API_KEY를 등록합니다. 값은 로컬 .env의 등호 뒤 문자열입니다.
3. Settings → Pages → Source를 GitHub Actions로 선택합니다.
4. Actions → Weekly Festival Data Update → Run workflow로 처음 한 번 실행합니다.
5. 성공한 배포와 온라인 페이지의 최근 업데이트 시간을 확인합니다.
GitHub에서 실행되므로 PC가 꺼져 있어도 업데이트됩니다. 예약 실행은 서버 상황에 따라 지연될 수 있습니다.
이 구성은 데이터 저장과 Pages 배포를 같은 워크플로에서 처리합니다.

## 일정의 범위
한국관광공사에 등록된 행사 기준이며 모든 축제를 보장하지 않습니다.
방문 전 주최 측 공지에서 운영 시간, 취소 여부, 주차, 예약 정보를 확인하세요.
'가족에게 적합함'이 검증된 행사만 따로 선별한 데이터는 아닙니다.
